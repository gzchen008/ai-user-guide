# 实战：天气查询工具

> 用 AI 开发你的第一个 API 调用应用

---

## 📋 项目概述

**项目目标：** 创建一个天气查询工具，可以查询任意城市的天气信息

**你将学到：**
- ✅ API 调用基础
- ✅ 异步编程（Async/Await）
- ✅ JSON 数据处理
- ✅ 错误处理
- ✅ 实时数据展示

**难度：** ⭐⭐（入门级）
**预计时间：** 2-3 小时
**推荐工具：** Cursor 或 Claude Code

---

## 🎯 功能需求

### 核心功能

1. **城市搜索**
   - 输入城市名称
   - 支持中英文城市名
   - 自动完成提示（可选）

2. **天气展示**
   - 当前温度
   - 天气状况（晴/雨/云等）
   - 湿度、风速
   - 天气图标

3. **多日预报**
   - 未来 3-7 天预报
   - 最高/最低温度
   - 天气趋势

### 进阶功能（可选）

4. **地理定位**
   - 自动获取当前位置
   - 显示当地天气

5. **天气地图**
   - 可视化展示天气

6. **天气预警**
   - 恶劣天气提醒

---

## 🛠️ 技术栈

**前端：**
- HTML5
- CSS3
- JavaScript (ES6+)

**API：**
- 免费天气 API（推荐：OpenWeatherMap / 和风天气）

**工具：**
- Cursor / Claude Code
- Postman（API 测试，可选）

---

## 📝 开发步骤

### 第 0 步：获取 API Key

**选择天气 API：**

#### 选项 1：OpenWeatherMap（推荐新手）

**优点：**
- 免费额度充足
- 文档清晰
- 支持中文城市

**注册步骤：**
1. 访问 https://openweathermap.org/api
2. 注册账号
3. 获取 API Key（免费版）
4. 等待几分钟激活

**免费额度：**
- 每天 1000 次调用
- 每分钟 60 次调用

---

#### 选项 2：和风天气（国内推荐）

**优点：**
- 国内访问快
- 中文支持好
- 数据更准确

**注册步骤：**
1. 访问 https://dev.qweather.com/
2. 注册账号
3. 创建应用获取 Key

**免费额度：**
- 每天 1000 次
- 支持商业项目

---

### 第 1 歃：创建项目结构

**在 Cursor 中新建项目：**

```
weather-app/
├── index.html      # 主页面
├── style.css       # 样式文件
├── app.js          # JavaScript 逻辑
└── config.js       # API 配置
```

**向 AI 提问：**
```
帮我创建一个天气查询应用的项目结构：
1. index.html - 主页面
2. style.css - 样式文件
3. app.js - 主要逻辑
4. config.js - API 配置（不要硬编码 API Key）
```

---

### 第 2 步：编写 HTML 结构

**向 AI 提问：**
```
帮我编写天气查询应用的 HTML 结构：
- 顶部标题 "天气查询"
- 搜索框和搜索按钮
- 当前天气展示区（温度、天气、图标、湿度、风速）
- 未来几天预报区
- 加载动画
- 错误提示区
```

**示例代码：**

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>天气查询工具</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <div class="container">
    <h1>🌤️ 天气查询</h1>
    
    <!-- 搜索区域 -->
    <div class="search-section">
      <input 
        type="text" 
        id="cityInput" 
        placeholder="输入城市名称（如：北京、Beijing）"
        autocomplete="off"
      >
      <button id="searchBtn">查询</button>
    </div>
    
    <!-- 加载动画 -->
    <div id="loading" class="loading hidden">
      <div class="spinner"></div>
      <p>正在查询天气...</p>
    </div>
    
    <!-- 错误提示 -->
    <div id="error" class="error hidden">
      <p id="errorMessage"></p>
    </div>
    
    <!-- 当前天气 -->
    <div id="currentWeather" class="current-weather hidden">
      <div class="weather-main">
        <h2 id="cityName"></h2>
        <div class="temp-section">
          <img id="weatherIcon" src="" alt="天气图标">
          <span id="temperature" class="temperature"></span>
        </div>
        <p id="weatherDesc" class="weather-desc"></p>
      </div>
      
      <div class="weather-details">
        <div class="detail-item">
          <span class="label">💧 湿度</span>
          <span id="humidity" class="value"></span>
        </div>
        <div class="detail-item">
          <span class="label">💨 风速</span>
          <span id="windSpeed" class="value"></span>
        </div>
        <div class="detail-item">
          <span class="label">🌡️ 体感</span>
          <span id="feelsLike" class="value"></span>
        </div>
        <div class="detail-item">
          <span class="label">👁️ 能见度</span>
          <span id="visibility" class="value"></span>
        </div>
      </div>
    </div>
    
    <!-- 未来预报 -->
    <div id="forecast" class="forecast hidden">
      <h3>未来 5 天预报</h3>
      <div id="forecastList" class="forecast-list">
        <!-- 预报项会动态插入这里 -->
      </div>
    </div>
  </div>
  
  <script src="config.js"></script>
  <script src="app.js"></script>
</body>
</html>
```

---

### 第 3 步：添加 CSS 样式

**向 AI 提问：**
```
帮我为天气应用添加现代美观的 CSS 样式：
- 渐变背景（根据天气变化）
- 卡片式设计
- 天气图标动画
- 响应式布局
- 加载动画
- 错误提示样式
```

**示例代码：**

```css
/* 全局样式 */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  min-height: 100vh;
  display: flex;
  justify-content: center;
  align-items: center;
  padding: 20px;
  transition: background 1s ease;
}

/* 天气背景变化 */
body.sunny {
  background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
}

body.rainy {
  background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
}

body.cloudy {
  background: linear-gradient(135deg, #a8edea 0%, #fed6e3 100%);
}

body.snowy {
  background: linear-gradient(135deg, #e0eafc 0%, #cfdef3 100%);
}

/* 主容器 */
.container {
  background: rgba(255, 255, 255, 0.95);
  border-radius: 20px;
  box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
  width: 100%;
  max-width: 600px;
  padding: 40px;
  backdrop-filter: blur(10px);
}

/* 标题 */
h1 {
  text-align: center;
  color: #333;
  margin-bottom: 30px;
  font-size: 32px;
}

/* 搜索区域 */
.search-section {
  display: flex;
  gap: 10px;
  margin-bottom: 30px;
}

#cityInput {
  flex: 1;
  padding: 14px 20px;
  border: 2px solid #e0e0e0;
  border-radius: 10px;
  font-size: 16px;
  outline: none;
  transition: border-color 0.3s;
}

#cityInput:focus {
  border-color: #667eea;
}

#searchBtn {
  padding: 14px 30px;
  background: #667eea;
  color: white;
  border: none;
  border-radius: 10px;
  font-size: 16px;
  cursor: pointer;
  transition: all 0.3s;
}

#searchBtn:hover {
  background: #5568d3;
  transform: translateY(-2px);
}

#searchBtn:active {
  transform: translateY(0);
}

/* 加载动画 */
.loading {
  text-align: center;
  padding: 40px;
}

.spinner {
  width: 50px;
  height: 50px;
  border: 4px solid #f3f3f3;
  border-top: 4px solid #667eea;
  border-radius: 50%;
  margin: 0 auto 20px;
  animation: spin 1s linear infinite;
}

@keyframes spin {
  0% { transform: rotate(0deg); }
  100% { transform: rotate(360deg); }
}

/* 错误提示 */
.error {
  background: #fff3f3;
  border: 1px solid #ff6b6b;
  border-radius: 10px;
  padding: 20px;
  text-align: center;
  color: #d63031;
}

/* 当前天气 */
.current-weather {
  margin-bottom: 30px;
}

.weather-main {
  text-align: center;
  margin-bottom: 30px;
}

#cityName {
  font-size: 28px;
  color: #333;
  margin-bottom: 20px;
}

.temp-section {
  display: flex;
  justify-content: center;
  align-items: center;
  gap: 20px;
  margin-bottom: 15px;
}

#weatherIcon {
  width: 100px;
  height: 100px;
  animation: bounce 2s infinite;
}

@keyframes bounce {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-10px); }
}

.temperature {
  font-size: 64px;
  font-weight: bold;
  color: #333;
}

.weather-desc {
  font-size: 20px;
  color: #666;
}

/* 天气详情 */
.weather-details {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 20px;
  background: #f9f9f9;
  padding: 20px;
  border-radius: 12px;
}

.detail-item {
  display: flex;
  flex-direction: column;
  gap: 5px;
}

.detail-item .label {
  font-size: 14px;
  color: #999;
}

.detail-item .value {
  font-size: 18px;
  font-weight: bold;
  color: #333;
}

/* 未来预报 */
.forecast h3 {
  font-size: 20px;
  color: #333;
  margin-bottom: 20px;
  text-align: center;
}

.forecast-list {
  display: grid;
  grid-template-columns: repeat(5, 1fr);
  gap: 15px;
}

.forecast-item {
  background: #f9f9f9;
  border-radius: 12px;
  padding: 15px;
  text-align: center;
  transition: transform 0.3s;
}

.forecast-item:hover {
  transform: translateY(-5px);
  background: #f0f0f0;
}

.forecast-date {
  font-size: 14px;
  color: #999;
  margin-bottom: 10px;
}

.forecast-icon {
  width: 50px;
  height: 50px;
  margin: 0 auto 10px;
}

.forecast-temp {
  font-size: 16px;
  font-weight: bold;
  color: #333;
}

.forecast-temp .high {
  color: #e74c3c;
}

.forecast-temp .low {
  color: #3498db;
  font-size: 14px;
  margin-left: 5px;
}

/* 工具类 */
.hidden {
  display: none !important;
}

/* 响应式 */
@media (max-width: 600px) {
  .container {
    padding: 20px;
  }
  
  h1 {
    font-size: 24px;
  }
  
  .search-section {
    flex-direction: column;
  }
  
  #searchBtn {
    width: 100%;
  }
  
  .forecast-list {
    grid-template-columns: repeat(3, 1fr);
  }
  
  .weather-details {
    grid-template-columns: 1fr;
  }
}
```

---

### 第 4 步：API 配置文件

**创建 config.js：**

```javascript
// API 配置
const CONFIG = {
  // 使用 OpenWeatherMap API
  API_KEY: 'YOUR_API_KEY_HERE', // 替换为你的 API Key
  BASE_URL: 'https://api.openweathermap.org/data/2.5',
  
  // 或使用和风天气 API
  // API_KEY: 'YOUR_QWEATHER_KEY_HERE',
  // BASE_URL: 'https://devapi.qweather.com/v7',
  
  // 默认城市
  DEFAULT_CITY: '北京',
  
  // 温度单位（metric: 摄氏度, imperial: 华氏度）
  UNITS: 'metric',
  
  // 语言（zh_cn: 中文, en: 英文）
  LANG: 'zh_cn'
};

// 注意：生产环境中，API Key 应该存储在后端，不要暴露在前端代码中
```

---

### 第 5 步：实现 JavaScript 逻辑

**向 AI 提问：**
```
帮我实现天气查询应用的 JavaScript 功能：
1. 调用 OpenWeatherMap API
2. 展示当前天气
3. 展示未来 5 天预报
4. 错误处理
5. 加载状态
6. 背景根据天气变化
```

**示例代码：**

```javascript
// DOM 元素
const cityInput = document.getElementById('cityInput');
const searchBtn = document.getElementById('searchBtn');
const loading = document.getElementById('loading');
const error = document.getElementById('error');
const errorMessage = document.getElementById('errorMessage');
const currentWeather = document.getElementById('currentWeather');
const forecast = document.getElementById('forecast');

// 获取当前天气
async function getCurrentWeather(city) {
  const url = `${CONFIG.BASE_URL}/weather?q=${encodeURIComponent(city)}&appid=${CONFIG.API_KEY}&units=${CONFIG.UNITS}&lang=${CONFIG.LANG}`;
  
  const response = await fetch(url);
  
  if (!response.ok) {
    if (response.status === 404) {
      throw new Error('找不到该城市，请检查城市名称');
    } else if (response.status === 401) {
      throw new Error('API Key 无效');
    } else {
      throw new Error('获取天气失败，请稍后重试');
    }
  }
  
  return await response.json();
}

// 获取天气预报
async function getForecast(city) {
  const url = `${CONFIG.BASE_URL}/forecast?q=${encodeURIComponent(city)}&appid=${CONFIG.API_KEY}&units=${CONFIG.UNITS}&lang=${CONFIG.LANG}`;
  
  const response = await fetch(url);
  
  if (!response.ok) {
    throw new Error('获取预报失败');
  }
  
  return await response.json();
}

// 显示当前天气
function displayCurrentWeather(data) {
  document.getElementById('cityName').textContent = `${data.name}, ${data.sys.country}`;
  document.getElementById('temperature').textContent = `${Math.round(data.main.temp)}°C`;
  document.getElementById('weatherDesc').textContent = data.weather[0].description;
  document.getElementById('humidity').textContent = `${data.main.humidity}%`;
  document.getElementById('windSpeed').textContent = `${data.wind.speed} m/s`;
  document.getElementById('feelsLike').textContent = `${Math.round(data.main.feels_like)}°C`;
  document.getElementById('visibility').textContent = `${data.visibility / 1000} km`;
  
  // 天气图标
  const iconCode = data.weather[0].icon;
  document.getElementById('weatherIcon').src = `https://openweathermap.org/img/wn/${iconCode}@2x.png`;
  
  // 根据天气改变背景
  changeBackground(data.weather[0].main);
  
  currentWeather.classList.remove('hidden');
}

// 显示天气预报
function displayForecast(data) {
  const forecastList = document.getElementById('forecastList');
  forecastList.innerHTML = '';
  
  // 获取每天中午的天气（每 3 小时一次数据，取中午 12 点的）
  const dailyData = {};
  
  data.list.forEach(item => {
    const date = new Date(item.dt * 1000);
    const dateStr = date.toDateString();
    const hour = date.getHours();
    
    if (!dailyData[dateStr] || Math.abs(hour - 12) < Math.abs(new Date(dailyData[dateStr].dt * 1000).getHours() - 12)) {
      dailyData[dateStr] = item;
    }
  });
  
  // 显示未来 5 天
  const forecasts = Object.values(dailyData).slice(0, 5);
  
  forecasts.forEach(item => {
    const date = new Date(item.dt * 1000);
    const dateStr = `${date.getMonth() + 1}/${date.getDate()}`;
    const iconCode = item.weather[0].icon;
    
    const forecastItem = document.createElement('div');
    forecastItem.className = 'forecast-item';
    forecastItem.innerHTML = `
      <div class="forecast-date">${dateStr}</div>
      <img class="forecast-icon" src="https://openweathermap.org/img/wn/${iconCode}@2x.png" alt="天气">
      <div class="forecast-temp">
        <span class="high">${Math.round(item.main.temp_max)}°</span>
        <span class="low">${Math.round(item.main.temp_min)}°</span>
      </div>
    `;
    
    forecastList.appendChild(forecastItem);
  });
  
  forecast.classList.remove('hidden');
}

// 根据天气改变背景
function changeBackground(weather) {
  const body = document.body;
  body.className = ''; // 清除所有类
  
  const weatherLower = weather.toLowerCase();
  
  if (weatherLower.includes('clear') || weatherLower.includes('晴')) {
    body.classList.add('sunny');
  } else if (weatherLower.includes('rain') || weatherLower.includes('drizzle') || weatherLower.includes('雨')) {
    body.classList.add('rainy');
  } else if (weatherLower.includes('cloud') || weatherLower.includes('云')) {
    body.classList.add('cloudy');
  } else if (weatherLower.includes('snow') || weatherLower.includes('雪')) {
    body.classList.add('snowy');
  }
}

// 显示加载状态
function showLoading() {
  loading.classList.remove('hidden');
  error.classList.add('hidden');
  currentWeather.classList.add('hidden');
  forecast.classList.add('hidden');
}

// 隐藏加载状态
function hideLoading() {
  loading.classList.add('hidden');
}

// 显示错误
function showError(message) {
  errorMessage.textContent = message;
  error.classList.remove('hidden');
  currentWeather.classList.add('hidden');
  forecast.classList.add('hidden');
}

// 搜索天气
async function searchWeather() {
  const city = cityInput.value.trim();
  
  if (!city) {
    showError('请输入城市名称');
    return;
  }
  
  showLoading();
  
  try {
    // 并行获取当前天气和预报
    const [currentData, forecastData] = await Promise.all([
      getCurrentWeather(city),
      getForecast(city)
    ]);
    
    hideLoading();
    displayCurrentWeather(currentData);
    displayForecast(forecastData);
    
  } catch (err) {
    hideLoading();
    showError(err.message);
  }
}

// 事件监听
searchBtn.addEventListener('click', searchWeather);

cityInput.addEventListener('keypress', (e) => {
  if (e.key === 'Enter') {
    searchWeather();
  }
});

// 初始化：加载默认城市天气
cityInput.value = CONFIG.DEFAULT_CITY;
searchWeather();
```

---

### 第 6 歃：测试和调试

**测试清单：**

```
□ 基本功能
  - 搜索国内城市（北京、上海）
  - 搜索国外城市（Tokyo、New York）
  - 中英文城市名都能识别
  
□ 错误处理
  - 输入不存在的城市
  - 网络断开时
  - API Key 错误时
  
□ 数据展示
  - 当前天气正确显示
  - 未来 5 天预报正确
  - 天气图标正确加载
  
□ 用户体验
  - 加载动画显示
  - 背景根据天气变化
  - 响应式布局正常
```

**调试技巧：**

1. **查看 API 响应**
   ```javascript
   // 在 fetch 后添加
   console.log('API Response:', data);
   ```

2. **检查网络请求**
   - Chrome 开发者工具 → Network
   - 查看请求和响应

3. **常见错误**
   ```
   错误：401 Unauthorized
   → API Key 错误或未激活
   
   错误：404 Not Found
   → 城市名称错误
   
   错误：CORS
   → 使用代理或后端转发
   ```

---

### 第 7 歃：进阶功能（可选）

#### 功能 1：添加地理定位

**向 AI 提问：**
```
帮我添加获取当前位置的功能：
- 页面加载时自动获取位置
- 显示当前位置的天气
- 添加"我的位置"按钮
```

**示例代码：**

```javascript
// 获取当前位置
function getCurrentLocation() {
  return new Promise((resolve, reject) => {
    if (!navigator.geolocation) {
      reject(new Error('浏览器不支持地理定位'));
      return;
    }
    
    navigator.geolocation.getCurrentPosition(
      position => {
        resolve({
          lat: position.coords.latitude,
          lon: position.coords.longitude
        });
      },
      error => {
        reject(new Error('无法获取位置，请手动输入城市'));
      }
    );
  });
}

// 根据坐标获取天气
async function getWeatherByCoords(lat, lon) {
  const url = `${CONFIG.BASE_URL}/weather?lat=${lat}&lon=${lon}&appid=${CONFIG.API_KEY}&units=${CONFIG.UNITS}&lang=${CONFIG.LANG}`;
  
  const response = await fetch(url);
  return await response.json();
}

// 在 HTML 中添加按钮
// <button id="locationBtn">📍 我的位置</button>

// 事件监听
document.getElementById('locationBtn').addEventListener('click', async () => {
  showLoading();
  
  try {
    const coords = await getCurrentLocation();
    const weatherData = await getWeatherByCoords(coords.lat, coords.lon);
    const forecastData = await getForecastByCoords(coords.lat, coords.lon);
    
    hideLoading();
    displayCurrentWeather(weatherData);
    displayForecast(forecastData);
  } catch (err) {
    hideLoading();
    showError(err.message);
  }
});
```

---

#### 功能 2：添加城市收藏

**向 AI 提问：**
```
帮我添加城市收藏功能：
- 可以收藏常用城市
- 快速切换查看
- 使用 LocalStorage 保存
```

---

#### 功能 3：添加天气图表

**向 AI 提问：**
```
帮我添加温度趋势图表：
- 使用 Chart.js 库
- 显示未来 5 天温度变化
- 美观的折线图
```

---

## 🚀 部署上线

### GitHub Pages 部署

**步骤：**

1. **上传到 GitHub**
   ```bash
   git init
   git add .
   git commit -m "天气查询应用"
   git remote add origin https://github.com/你的用户名/weather-app.git
   git push -u origin main
   ```

2. **重要：保护 API Key**
   - 不要把真实的 API Key 提交到 GitHub
   - 使用环境变量或后端代理
   - 在 `.gitignore` 中添加 `config.js`

3. **启用 GitHub Pages**
   - Settings → Pages → Source: main

---

## ⚠️ 注意事项

### 1. API Key 安全

```
⚠️ 不要在前端暴露 API Key！

解决方案：
1. 使用后端代理
2. 使用环境变量
3. 限制 API Key 的域名访问
4. 设置 API 调用限额
```

### 2. API 限制

```
免费版限制：
- 每天调用次数有限
- 每分钟调用次数有限
- 某些高级功能不可用

建议：
- 添加缓存（避免重复请求）
- 添加防抖（避免频繁请求）
- 错误处理友好提示
```

### 3. 跨域问题

```
如果遇到 CORS 错误：

解决方案：
1. 使用支持 CORS 的 API
2. 搭建后端代理
3. 使用 CORS 代理服务
```

---

## 💡 学到了什么

完成这个项目后，你应该掌握了：

1. **API 调用**
   - Fetch API 使用
   - 异步编程（Async/Await）
   - Promise 并行处理

2. **数据处理**
   - JSON 解析
   - 数据过滤和转换
   - 动态渲染 DOM

3. **错误处理**
   - Try-Catch 使用
   - 用户友好的错误提示
   - 网络错误处理

4. **用户体验**
   - 加载状态
   - 响应式设计
   - 视觉反馈

---

## 🎯 下一步

完成这个项目后，可以尝试：

1. **改进当前项目**
   - 添加更多天气信息
   - 优化用户界面
   - 添加天气预警

2. **挑战新项目**
   - [AI 聊天助手](./04-AI聊天助手.md) - 调用 AI API
   - [数据分析工具](./05-数据分析.md) - 数据可视化

3. **学习新技术**
   - 后端开发（Node.js）
   - 数据库（MongoDB）
   - 框架（Vue/React）

---

## ❓ 常见问题

<details>
<summary><b>Q: API Key 可以给别人用吗？</b></summary>

**A:** 不建议。

风险：
- 别人可能滥用导致超额
- 可能用于恶意目的
- 你的账号可能被封

建议：
- 设置域名白名单
- 设置调用限额
- 定期监控使用情况
</details>

<details>
<summary><b>Q: 如何处理 API 调用失败？</b></summary>

**A:** 多层防护：

1. **Try-Catch 捕获错误**
2. **重试机制**（失败后自动重试）
3. **降级方案**（显示缓存数据）
4. **友好提示**（告诉用户怎么办）
</details>

<details>
<summary><b>Q: 免费API够用吗？</b></summary>

**A:** 个人使用通常够用。

免费额度：
- OpenWeatherMap: 1000次/天
- 和风天气: 1000次/天

建议：
- 添加本地缓存
- 避免重复请求
- 监控使用量
</details>

---

**💡 小贴士：** API 调用是现代 Web 开发的核心技能。掌握这个项目，你就可以接入各种第三方服务（地图、支付、AI 等），开发更强大的应用！

---

## 📚 相关链接

- [OpenWeatherMap API 文档](https://openweathermap.org/api)
- [和风天气开发文档](https://dev.qweather.com/doc/)
- [Fetch API 教程](https://developer.mozilla.org/zh-CN/docs/Web/API/Fetch_API)
- [Async/Await 详解](https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Statements/async_function)
